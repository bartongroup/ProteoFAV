# This requirements file lists all third-party dependencies for this project.
#
# Run 'pip install -r requirements.txt' to install these dependencies
scipy
numpy
requests
pandas>=0.17
lxml
biopython

# Testing
mock
responses

# Protein structure visualisation
#pymol (not in pip; available through brew on Mac)